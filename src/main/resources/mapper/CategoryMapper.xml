<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todo.mapper.CategoryMapper">

    <select id="findAll" resultType="com.example.todo.model.Category">
        SELECT id, name, color
        FROM categories
        ORDER BY id
    </select>

    <select id="findById" parameterType="long" resultType="com.example.todo.model.Category">
        SELECT id, name, color
        FROM categories
        WHERE id = #{id}
    </select>

</mapper>
